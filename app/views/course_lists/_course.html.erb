<% @courses.each do |course| %>
  <h2><%= course.course_title %></h2>
<h3><%= course.teacher %></h3>
<% if course.quantity <= Course::MAXIMUM %>
  <%= button_to "Select Course", line_items_path(course_id: course) %>
  <% else %>
  <%= link_to 'full' %>
  <% end %>
  <% end %>
  <br><br>

