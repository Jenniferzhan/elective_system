<% if session[:user_id] %>
  <p>welcome, <%= Student.find(session[:user_id]).name %>
<% end %>
<h1>Course Lists</h1>

<% @courses.each do |course| %>
  <h2><%= course.course_title %>
  <h3><%= course.teacher %>
  <%= button_to "Select Course", line_items_path(course_id: course) %>
<% end %>
